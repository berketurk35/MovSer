import { StyleSheet, Dimension } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: "row",
        margin: 10,
    },
    cardName: {
        flex: 1,
        fontSize: 14,
        fontWeight: "bold",
    },
    icon: {
        justifyContent: "center",
    }
});